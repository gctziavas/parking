spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/spacedrop
          jwk-set-uri: http://localhost:8180/realms/spacedrop/protocol/openid-connect/certs
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false

eureka:
  client:
    enabled: false
